### Summary

The `sandwich_attack` article contrasts the attack with front-running twice:

- `## Distinction from Front-Running and Back-Running` — front-running is before-only, back-running is after-only, a sandwich wraps both sides.
- `## Distinction from Front-Running` — restates that front-running acts first while a sandwich brackets the victim, so front-running is one half of the sandwich.

The standalone `Distinction from Front-Running` duplicates the front-running half of the combined section.

### Proposed fix

Fold the unique "front-running is one half of the sandwich rather than the whole maneuver" framing into `## Distinction from Front-Running and Back-Running` with inline citations, and remove the redundant `## Distinction from Front-Running` section.

### File

- `content/pages/sandwich_attack/index.mdx`
