---
title: "Burn Increase Mult"
summary:
  "How the burn increase mult subnet hyperparameter raises a subnet's dynamic registration burn cost
  each time a registration succeeds, making registration more expensive as demand rises."
category: "Subnets"
tags: ["Subnets", "Registration"]
infoboxTitle: Burn Increase Mult
infoboxCaption: Step-up factor applied to the registration burn on each registration.
infoboxRows:
  - label: Parameter
    value: BurnIncreaseMult
  - label: Context
    value: Registration burn
  - label: Trigger
    value: Successful registration
  - label: Example
    value: netuid 1
  - label: Set by
    value: Subnet owner
---

# Burn Increase Mult

Burn increase mult is a per-subnet hyperparameter, listed in the documentation as
`BurnIncreaseMult`, that raises a subnet's dynamic registration burn cost when a registration
succeeds. The
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters) reference
describes it as the factor by which the current neuron registration burn cost is multiplied when a
registration succeeds, which is what makes registration more expensive as demand rises.

## What It Controls

The hyperparameter sets the step-up applied to the live
[burn cost](https://docs.learnbittensor.org/resources/glossary#burn-cost) after each successful
registration. Registration burns TAO, and that cost is dynamic: it rises with registrations and
eases back during quieter periods. Burn increase mult names the upward side of that movement, while
[burn half life](https://docs.learnbittensor.org/subnets/subnet-hyperparameters) names the downward
decay ([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

When registrations cluster, each success multiplies the burn upward, which raises the price of the
next slot and throttles a registration rush. It does not set the floor or ceiling of the burn; those
are the separate min burn and max burn hyperparameters
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Because the step-up fires only after a successful registration rather than at tempo boundaries,
several increases can land within one consensus cycle when registrations cluster inside a single
epoch. Burn half life eases the live burn every block between those events; burn increase mult
raises it only at registration boundaries
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register),
[Glossary: Tempo](https://docs.learnbittensor.org/resources/glossary#tempo)).

## On-Chain Application

On chain, the step-up is applied as a direct multiply of the live burn immediately after each
successful registration. The effective multiplier is floored at one, so a configured value below one
cannot lower the burn; the multiplied result is then clamped back inside the `MinBurn` and `MaxBurn`
band before it takes effect
([`bump_registration_price_after_registration`](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/registration.rs)).

This per-registration bump is skipped for the root subnet, which keeps interval-based admission
rather than the per-block decay and per-registration step-up used by other subnets. So burn increase
mult shapes registration price on ordinary subnets, not on root
([`update_registration_prices_for_networks`](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/registration.rs)).

## Documented Description and Setter

The [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
reference lists `BurnIncreaseMult` as a registration-burn hyperparameter and describes it as the
factor by which the current burn is multiplied on a successful registration. The step-up factor on
netuid 1 can differ from another subnet's multiplier
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Unlike the burn bounds `MinBurn` and `MaxBurn`, which carry fixed documented defaults, the reference
does not publish a single default for `BurnIncreaseMult`. It records the value as network-dependent
and directs readers to read it from the live hyperparameter table rather than assume a constant,
which is why the infobox shows no default figure
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The reference also documents it as a floating-point multiplier in the runtime rather than a
normalized integer, so it is read as a plain factor applied to the current burn rather than a value
that must be scaled like the TAO-denominated burn bounds
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The value in force is per-subnet chain state, so a subnet's real step-up behavior should be read for
that subnet rather than assumed. For the documented example netuid 1 on Finney mainnet it can be
read with `btcli subnet hyperparameters --netuid 1 --network finney`, which keeps the multiplier
claim reproducible against a parseable netuid
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).

## Distinction from Burn Half Life

Burn increase mult steps the live burn up on each registration, while burn half life decays it back
toward the floor between registrations
([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Burn increase mult** — raises the burn when a registration succeeds.
- **Burn half life** — decays the burn back down during quiet periods.

## Distinction from Min Burn and Max Burn

Min burn and max burn fix the lower and upper bounds of the registration-burn band, while burn
increase mult moves the live burn upward inside that band as registrations occur. Max burn in
particular caps the upper bound the multiplier can climb toward, so each successful registration
steps the live burn higher without ever exceeding that ceiling
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Mining: Miner registration](https://docs.learnbittensor.org/miners#miner-registration),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Min burn / max burn** — the floor and ceiling of the burn range.
- **Burn increase mult** — the per-registration step that climbs within that range toward the
  max-burn ceiling.

## Distinction from Registration

Burn increase mult shapes how the dynamic registration burn responds to demand. Neuron registration
is the action that triggers the step-up by paying the current burn to claim a UID slot. On ordinary
subnets such as netuid 1, admission is continuous without interval-based registration windows, and
the hyperparameter reference marks `MaxRegistrationsPerBlock` as not used for live neuron
registration ([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register),
[Miner registration](https://docs.learnbittensor.org/miners#miner-registration),
[Subnet Hyperparameters: MaxRegistrationsPerBlock](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxregistrationsperblock)).

- **Neuron registration** — claiming a UID slot by paying the dynamic registration burn.
- **Burn increase mult** — factor that multiplies the burn upward after each successful
  registration.

## Distinction from Max Registrations Per Block

Burn increase mult is a pricing step that multiplies the live registration burn upward after each
successful registration on a subnet such as netuid 1. Max registrations per block is legacy
hyperparameter vocabulary for a per-block ceiling on neuron registrations under older models. The
reference now states that cap is not used for live neuron registration; continuous burn pricing
through burn increase mult and burn half life governs admission instead
([Subnet Hyperparameters: MaxRegistrationsPerBlock](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxregistrationsperblock),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Pricing-step vocabulary and legacy throughput-cap vocabulary answer different registration
questions. When several registrations cluster in one block, burn increase mult still steps the live
burn after each accepted registration inside the min/max band; max registrations per block names an
obsolete per-block burst limit rather than the live admission gate on ordinary subnets today
([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

- **Burn increase mult** — per-registration step-up applied to the live burn price.
- **Max registrations per block** — legacy per-block registration cap vocabulary.

## Chain Reads for netuid 1

Readers can verify live hyperparameter values for the documented example netuid with
`btcli subnet hyperparameters --netuid 1 --network finney`
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).

That read path keeps live hyperparameter claims tied to a parseable netuid.

## Per-Subnet Live Value Boundary

Burn increase mult is per-subnet chain state. The step-up multiplier configured on one netuid
controls how sharply registration burn rises after each successful registration on that subnet, but
the live value can differ from reference defaults and from the multiplier on another netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost)).

This article's infobox uses netuid 1 as an example label when reading one subnet's hyperparameter
table on Finney mainnet. That example helps readers verify one row; it is not proof that every
subnet exposes the same live step-up multiplier
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

Burn increase mult names the per-registration increase inside the burn band; Yuma Consensus still
settles validator weights into emission shares each epoch on the active subnet
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

- **Burn increase mult** — per-registration burn step-up multiplier on a subnet.
- **Per-subnet boundary** — live multiplier is chain state on one netuid.

## Distinction from Yuma Consensus

Burn increase mult is a per-subnet hyperparameter that steps the dynamic registration burn upward
after each successful registration. Yuma Consensus is the separate tempo process that converts
validator weight submissions into emission shares each epoch
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

- **Burn increase mult** — per-registration multiplier toward the burn ceiling.
- **Yuma Consensus** — on-chain settlement that turns validator weights into emission shares. Burn
  increase mult names registration burn stepping; Yuma processes validator weights into rewards
  ([Yuma Consensus: Validator emissions](https://docs.learnbittensor.org/learn/yuma-consensus#validator-emissions)).

- **Registration vs settlement** — burn increase mult paces registration economics; Yuma aggregates
  validator weights into emission shares at epoch boundaries
  ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
  [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).
- **Demand response** — clustered registrations step burn upward inside the min/max band
  ([Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

## Reader Boundary

Burn increase mult should not be read as a fixed registration price, as the min or max burn bounds,
or as a live per-block registration throughput cap. It names only the per-registration step-up
applied to the live burn on a selected netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Subnet Hyperparameters: MaxRegistrationsPerBlock](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxregistrationsperblock)).

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Subnet Hyperparameters: MaxRegistrationsPerBlock](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#maxregistrationsperblock)
- [Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)
- [Glossary: Burn Cost](https://docs.learnbittensor.org/resources/glossary#burn-cost)
- [Miner registration](https://docs.learnbittensor.org/miners#miner-registration)
