---
title: "Subnet 21: AdTAO"
summary:
  "AdTAO is a Bittensor subnet that runs a verifiable prediction market for Google Ads outcomes,
  rewarding miners for calibrated forecasts of advertising intervention effects scored against
  on-chain-sealed ground truth."
category: "Subnets"
tags: ["Subnets"]
infoboxTitle: Subnet 21 — AdTAO
infoboxCaption: Verifiable prediction market for Google Ads campaign outcomes
infoboxRows:
  - label: Subnet
    value: SN21
  - label: Operator
    value: PPC Rebel
  - label: Neurons
    value: "256"
  - label: GitHub
    value: ippcteam/SN21-adtao
  - label: Site
    value: ppcrebel.com
---

AdTAO is Bittensor Subnet 21, operated by the team behind [PPC Rebel](https://ppcrebel.com). The
subnet functions as a verifiable prediction market for Google Ads campaign outcomes. Its core task
is counterfactual impact prediction: given a structured description of an advertising account and an
action that has been applied to it, miners forecast how the account's key metrics will shift over
the following 7 and 14 days. Every prediction is timelock-encrypted and committed on chain before
the outcome is measurable, and every scoring step is anchored to on-chain data that any observer can
independently verify. The full protocol is documented in the
[SN21 GitHub repository](https://github.com/ippcteam/SN21-adtao).

## How the Mechanism Works

Each prediction cycle in AdTAO is organized around an **episode** — a structured package of data
describing a real Google Ads account at a specific moment. An episode contains the account's 60-day
historical campaign time series, metadata about its current goal and spend tier, and an **action
bundle** describing the intervention whose effect is being predicted. Phase 1 covers four action
types: budget changes, bid strategy switches, target value adjustments such as changes to a target
cost-per-acquisition or target return on ad spend, and campaign pauses. The full list of supported
action types is defined in
[`hope/constants.py`](https://github.com/ippcteam/SN21-adtao/blob/main/hope/constants.py) in the
public repository.

Miners receive the episode and return **probabilistic distributions** — P10, P50, and P90 estimates
— for how the primary goal metric will change at each prediction horizon. The task demands
calibrated uncertainty rather than a single point estimate. Miners who are overconfident or
underconfident are penalized accordingly.

To prevent prediction manipulation, miner submissions are protected by a two-layer encryption
scheme. Each prediction archive is encrypted with AES-GCM. The AES key is then timelock-encrypted to
a future round of the [drand quicknet beacon](https://drand.love), a distributed randomness beacon
run by the League of Entropy. The ciphertext hash, the locked key, and an archive URL are committed
on chain at submission time. This construction makes retroactive changes to a prediction detectable
from chain state alone; no one, including the subnet operator, can alter a submitted prediction
without leaving a verifiable trace.

Validators retrieve predictions after the timelock reveals, verify each submission passes an
eight-check scoreability rule, and score miners across four components. Quantile accuracy carries
50% of the total score and is measured via pinball loss and the Continuous Ranked Probability Score
applied to the P10/P50/P90 distributions against actual outcomes. Calibration contributes 20% and
evaluates interval coverage with a convex width penalty that discourages over-wide prediction
intervals. Directional accuracy carries 15% and checks whether the predicted sign of change matches
what actually happened. Goal accuracy makes up the remaining 15% and applies a Brier score to the
probability the miner assigned to the campaign missing its stated goal. A miner whose aggregate
score fails to exceed the conditional-prior baseline — essentially a simple historical reference
model — earns zero emission for that epoch regardless of absolute score. This baseline requirement
is documented in the
[reward mechanism specification](https://github.com/ippcteam/SN21-adtao/blob/main/docs/SN21_REWARD_MECHANISM.md).

Emissions follow a tiered structure. According to
[SubnetRadar's coverage of the mainnet launch](https://subnetradar.com/subnet-news/21/2026-05-19),
the top 20% of miners by score receive 60% of available emissions per epoch, the middle 40% receive
30%, and the bottom 40% receive 10%. The subnet launched with a 75% burn rate directed to UID 0.

A shadow validator runs the same open-source scoring code on a separate hotkey and commits its own
artifacts each epoch. Any divergence between the primary and shadow scorer is publicly auditable.
Because every scoring input is committed to chain as a Merkle root, anyone can re-run the
open-source verifier script against any past epoch and either confirm or contradict the validator's
reported scores.

## Participating as a Miner

Miners on Subnet 21 compete by submitting calibrated probabilistic forecasts for Google Ads campaign
metric changes. Effective participation requires a model that can ingest structured account
snapshots — including historical spend and conversion time series, bid strategy context, and the
specific action being applied — and produce well-calibrated P10/P50/P90 distributions for two time
horizons.

The baseline requirement means that miners whose models fail to outperform a simple
conditional-prior reference earn no emission, regardless of submission volume. Prospective miners
should review the
[miner quickstart guide](https://github.com/ippcteam/SN21-adtao/blob/main/docs/miner_quickstart.md)
and the [whitepaper](https://github.com/ippcteam/SN21-adtao/blob/main/docs/whitepaper.md) in the
repository before registering a hotkey. Hardware requirements for running a miner node are listed in
the repository README and are modest relative to compute-intensive subnets, as the primary task is
inference rather than model training.

## Participating as a Validator

Validators on AdTAO retrieve miner prediction archives after each timelock reveal, apply the
scoreability filter, and run the open-source scoring library against the released outcomes. The
scoring code has no Bittensor dependency and can be audited and tested independently. Validators
then submit timelocked weight commits through the standard Bittensor commit-reveal mechanism
described in [[Yuma Consensus]].

The shadow-validator design means that primary validator scoring is cross-checked each epoch by a
second independent run of the same code. Validators whose weights diverge significantly from
consensus are penalized through the standard [[Yuma Consensus]] accountability mechanism. Detailed
setup instructions are available in the [SN21 repository](https://github.com/ippcteam/SN21-adtao).

## On-Chain Identity

Subnet 21 is registered on the Bittensor mainnet under the name AdTAO, operated by PPC Rebel under
owner coldkey `5HjCYVfrWSkzTfJM5rkWBW3qTTJqXEFUzZrKty5hodpgfjyW`. The subnet supports 256 neurons.
The incentive mechanism code is publicly maintained at
[github.com/ippcteam/SN21-adtao](https://github.com/ippcteam/SN21-adtao), and the product site is at
[ppcrebel.com](https://ppcrebel.com). Live on-chain data including metagraph state, neuron counts,
and alpha token pool information is available at
[taostats.io/subnets/21](https://taostats.io/subnets/21).

## Further Reading

- [AdTAO product site — ppcrebel.com](https://ppcrebel.com)
- [SN21 GitHub repository](https://github.com/ippcteam/SN21-adtao)
- [SN21 reward mechanism specification](https://github.com/ippcteam/SN21-adtao/blob/main/docs/SN21_REWARD_MECHANISM.md)
- [Subnet 21 on Taostats](https://taostats.io/subnets/21)
- [Subnet 21 on bittensor.ai](https://bittensor.ai/subnets/21)
- [[Yuma Consensus]]
- [[Dynamic TAO]]
- [[Mining and Validating]]
