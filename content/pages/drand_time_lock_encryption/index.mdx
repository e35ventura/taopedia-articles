---
title: "Drand Time-Lock Encryption"
summary:
  "How Drand time-lock encryption supports delayed validator-weight visibility in Bittensor Commit
  Reveal."
category: "Consensus"
tags: ["Consensus", "Cryptography"]
infoboxTitle: Drand Time-Lock Encryption
infoboxCaption: Delayed access to concealed validator weights.
infoboxRows:
  - label: Used by
    value: Commit Reveal
  - label: Protects
    value: Fresh validator weights
  - label: Timing source
    value: Drand rounds
  - label: Scope
    value: Delayed visibility
---

# Drand Time-Lock Encryption

Drand time-lock encryption helps Bittensor Commit Reveal keep newly submitted validator weights
concealed until a later reveal round. In the commit-then-reveal flow, validators commit weight
information before it becomes readable, reducing the usefulness of copied public weights
([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)).

The term belongs to timing and visibility. It explains when committed validator-weight information
can be read, while validator weights and Yuma Consensus explain how revealed evaluations are used
after the delay
([Glossary: Validator Weights](https://docs.learnbittensor.org/resources/glossary#validator-weights),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

## Delayed Visibility

Delayed visibility matters because validator weights are valuable consensus inputs. A validator that
can simply reuse public weight signals adds less independent evaluation to a subnet
([The Weight Copying Problem](https://docs.learnbittensor.org/concepts/weight-copying-in-bittensor)).

Commit Reveal reduces that copying window by separating submission from later readability. The
validator commits information first, and the readable weight information appears later in the reveal
part of the process ([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal)).

That delay is useful because the valuable signal is the fresh validator evaluation, not a public
copy of another validator's recent weights. Drand time-lock encryption is the timing tool that helps
preserve that delay.

## Drand Round Timing

Time-lock encryption binds access to a later round instead of immediate decryption. Encrypted data
becomes decryptable only after a future Drand round
([Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)).

The Drand round supplies the timing reference. Before that round, the committed weight information
stays concealed; after the relevant round, the reveal process can make the information readable.

This makes the phrase more specific than generic encryption. The important feature is delayed
readability tied to a future Drand round, not merely that the data was encrypted.

## Commit Reveal

Commit Reveal uses concealed weight submission before later reveal. Drand time-lock encryption fits
that process as the timing layer: committed weight data remains unreadable until the target round,
then the revealed information can enter the consensus flow
([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)).

That placement keeps the concept narrow. Commit Reveal is the broader commit-then-reveal process;
Drand time-lock encryption is the delayed-readability tool used inside that process.

## Consensus Role

Drand time-lock encryption controls when concealed weight information becomes readable; it does not
score miners or allocate emissions. After reveal, validator evaluations enter the Yuma Consensus
aggregation process for miner incentives and validator dividends
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal)).

The delayed-visibility layer protects inputs before that aggregation step. Once the weights are
revealed, nearby terms such as validator weights, rank, incentives, and dividends describe the
consensus and emission path.

## Reading the Term

The term names a timing and visibility tool, not a separate consensus algorithm. It is closest to
Commit Reveal, weight-copying protection, and validator-weight handling
([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[The Weight Copying Problem](https://docs.learnbittensor.org/concepts/weight-copying-in-bittensor)).

The concise reading is: Drand time-lock encryption delays readability of committed validator-weight
information until a future Drand round, helping Commit Reveal reduce copied-weight behavior before
Yuma Consensus uses the revealed weights.

## Beacon Pulse Unlocks Concealed Weights

Drand round numbers describe when concealed validator-weight information can become readable, but
the unlock step also depends on the beacon pulse from the Drand network. Official Commit Reveal
documentation states that the chain reveals committed weights when the corresponding Drand beacon
pulse becomes available and supplies the cryptographic key needed to unlock the time-locked
encryption ([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal)).

That pulse is more specific than a generic future timestamp. The committed encrypted weight data may
already exist on chain, but the beacon output is what makes decryption possible at the intended
moment
([Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)).

Round timing and beacon pulse therefore answer different parts of the same unlock event. A Drand
round names the schedule anchor, while the pulse names the randomness output that satisfies the
time-lock condition for concealed validator-weight information.

This makes Drand time-lock encryption easier to read beside Commit Reveal prose. The mechanism
concerns when decryption becomes possible, and the beacon pulse is the documented trigger for that
unlock step.

References: [Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)

## Concealment Length From Subnet Settings

The concealment interval before reveal is configured per subnet rather than implied by Drand alone.
Commit Reveal documentation describes a waiting period set through the commit_reveal_period
hyperparameter and measured in tempos, with subnet owners enabling and tuning the mechanism through
subnet hyperparameter settings
([Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

During that configured interval, committed weight information stays encrypted on chain before the
automatic reveal step runs. The hyperparameter value defines how many tempos pass before that reveal
timing is reached, while Drand supplies the external unlock reference used at reveal time.

A timing claim should therefore name the selected subnet setting as well as the Drand unlock step.
Two subnets can share the same Drand infrastructure but use different concealment lengths because
their commit_reveal_period values differ
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Subnet hyperparameter documentation groups Commit Reveal with other subnet settings that shape
validator signal flow and epoch timing. That placement helps read the concealment interval as part
of subnet configuration rather than as a standalone cryptography label.

References: [Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)

## Weight Matrix Follows Reveal

The weight matrix collects validator weight vectors as structured input for Yuma Consensus after
those signals are available
([Glossary: Weight Matrix](https://docs.learnbittensor.org/resources/glossary#weight-matrix),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

Drand time-lock encryption sits earlier in the validator-weight path. It governs when concealed
forms of validator weight information can be decrypted inside Commit Reveal, not how revealed
vectors are organized into subnet-level matrix form
([Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)).

That order keeps timing vocabulary separate from input-structure vocabulary. Concealed submission
and timed reveal belong to Drand time-lock encryption language; collection of revealed validator
vectors for Yuma processing belongs to weight matrix language
([Glossary: Weight Matrix](https://docs.learnbittensor.org/resources/glossary#weight-matrix)).

References:
[Glossary: Weight Matrix](https://docs.learnbittensor.org/resources/glossary#weight-matrix),
[Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)

## Further Reading

- [Introduction to Bittensor: Subnet development](https://docs.learnbittensor.org/learn/introduction#subnet-development)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
- [Commit Reveal](https://docs.learnbittensor.org/concepts/commit-reveal)
- [Glossary: Drand/time-lock encryption](https://docs.learnbittensor.org/resources/glossary#drandtime-lock-encryption)
- [The Weight Copying Problem](https://docs.learnbittensor.org/concepts/weight-copying-in-bittensor)
- [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)
