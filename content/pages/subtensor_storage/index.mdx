---
title: "Subtensor Storage"
summary: "How Subtensor runtime storage entries organize Bittensor chain state by runtime area."
category: "Core Concepts"
tags: ["Subtensor", "Reference"]
infoboxTitle: Subtensor Storage
infoboxCaption: Runtime storage entries in Subtensor.
infoboxRows:
  - label: Layer
    value: Subtensor runtime
  - label: Form
    value: Storage entries
  - label: Organized by
    value: Runtime area
  - label: Purpose
    value: Chain state reference
---

# Subtensor Storage

Subtensor storage entries are runtime state definitions exposed by the Subtensor runtime
([Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

The term belongs to chain-state reference vocabulary. It should be read separately from events,
constants, and submitted runtime actions.

## What Storage Entries Represent

Storage entries name places where runtime state is exposed for inspection. A storage reference can
identify the runtime area, entry name, type, lookup shape, and summary for a state surface
([Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Subtensor Constants](https://docs.learnbittensor.org/subtensor-api/constants)).

This makes storage a map of exposed runtime state rather than a full explanation of every mechanism
that changes that state.

The entry definition is stable reference material. A value read from that entry is separate evidence
tied to the network and block context where the read happened.

That distinction keeps the article from treating a reference table as a complete chain-state claim.
The storage page names the state surface; a reader still needs context before interpreting a
returned value.

## Runtime Areas

Storage entries are grouped by runtime area. The owning area is part of the entry's meaning because
the same kind of state label can carry different context in different runtime areas
([Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

For readers, a storage item should first be read through its owning area, then through its entry
name and type.

Runtime-area context also helps separate similar-looking entries. A name that appears under one area
should not be generalized to another area unless the source connects those surfaces directly.

This also makes the runtime area part of citation quality. A source-backed statement about one
storage area should stay attached to that area rather than becoming a broad claim about every
Subtensor state surface.

## Definitions and Queried Values

The storage reference defines where state is exposed. A chain read or inspection view can show a
value from a selected network and block context
([Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

This distinction matters for evidence. The reference gives the map; an observed value belongs to the
chain context where it was read.

A definition can support claims about what state surface exists. A queried value can support claims
about what that surface returned in a particular context. Mixing those two evidence types can make a
reference article sound more certain than the source allows.

## Relationship to Events and Extrinsics

Storage, events, and extrinsics describe different surfaces. Storage exposes state, events name
records emitted during processing, and extrinsics name submitted runtime actions
([Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events),
[Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)).

A feature may involve all three surfaces, but they should not be collapsed into one evidence type.

The distinction is useful when reading Subtensor references together. Storage can describe durable
state surfaces, events can describe emitted records, and extrinsics can describe submitted actions
that may change state.

For a reader, this prevents evidence drift. A storage entry can support a claim about exposed state,
an event can support a claim about emitted activity, and an extrinsic can support a claim about a
submitted action.

## Network and Block Scope

Storage values are scoped by chain history. The same storage entry name can be meaningful across
environments while returned values belong to separate network and block contexts
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks),
[Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)).

When precision matters, the network and inspection context should stay attached to the claim.

Examples should also keep their environment label attached. A localnet or testnet storage example
can illustrate how an entry is read, but it should not be treated as evidence for another network
environment.

That framing is enough for a concept article. The important boundary is that Subtensor storage
defines exposed state surfaces, while observed values remain tied to the environment and block
context where they were observed.

## Reader Boundary

Subtensor storage should be read as chain-state reference vocabulary. It is not a full mechanism
guide, proof of off-chain intent, or replacement for concept documentation
([Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage),
[Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)).

The stable article-level point is that Subtensor storage names runtime state surfaces exposed for
inspection.

The article should therefore avoid presenting storage names as instructions. The source-backed
definition is about what the runtime exposes, not about which value a reader should query for a
particular decision.

## Further Reading

- [Subtensor Storage](https://docs.learnbittensor.org/subtensor-api/storage)
- [Subtensor Events](https://docs.learnbittensor.org/subtensor-api/events)
- [Subtensor Constants](https://docs.learnbittensor.org/subtensor-api/constants)
- [Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics)
- [Inspecting the Chain with Polkadot.js](https://docs.learnbittensor.org/concepts/inspecting-the-chain)
- [Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)
