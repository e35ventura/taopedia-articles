---
title: "Bonds Reset Enabled"
summary:
  "How the bonds reset enabled subnet hyperparameter gates whether accumulated validator-miner bond
  state may be cleared on a subnet."
category: "Consensus"
tags: ["Consensus", "Subnets"]
infoboxTitle: Bonds Reset Enabled
infoboxCaption: Boolean gate for bond-reset capability.
infoboxRows:
  - label: Parameter
    value: BondsResetEnabled
  - label: Type
    value: Bool
  - label: Default
    value: "False"
  - label: Example
    value: netuid 1
  - label: Set by
    value: Subnet owner
---

# Bonds Reset Enabled

Bonds reset enabled is a per-subnet hyperparameter, listed in the documentation as
`BondsResetEnabled`, that gates whether
[validator-miner bonds](https://docs.learnbittensor.org/resources/glossary#validator-miner-bonds) on
a subnet are reset-enabled. The reference describes it as determining whether bonds are
reset-enabled, with a default of false so accumulated bond history ordinarily persists unless an
owner enables the reset path
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controls

The hyperparameter is a boolean capability switch, not a bond strength or smoothing setting. It
decides whether the subnet allows bond-reset behavior when qualifying events occur, rather than
keeping every accumulated bond entry unchanged
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus: Bonding mechanics](https://docs.learnbittensor.org/learn/yuma-consensus#bonding)).

Validator-miner bonds are smoothed relationships that evolve across Yuma Consensus epochs and feed
into [validator dividends](https://docs.learnbittensor.org/resources/glossary#emission). When reset
is disabled, that history continues through ordinary neuron changes; when reset is enabled, the
subnet can clear stale bond entries instead of carrying them forward unchanged
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

The flag is owner-tunable per subnet, so netuid 1 can keep the default while another netuid enables
resets, and the live value is chain state rather than a network constant
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

## Documented Type and Default

The [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
reference lists `BondsResetEnabled` as a `Bool` with a default of false and marks the permission
required to change it as subnet owner
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

With the default off, subnets preserve accumulated bond history unless an owner explicitly enables
the reset path for that netuid.

## Reading the Live Value for netuid 1

Bonds reset enabled is per-subnet chain state, so any live claim is tied to a specific netuid. For
the documented example netuid 1 on Finney mainnet, the flag appears in that subnet's hyperparameter
table, which the reference describes under viewing a subnet's hyperparameters
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).
The infobox uses netuid 1 only as an example label; other subnets expose their own values
([Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

## Distinction from Bonds Moving Average

Bonds moving average sets how quickly validator-miner bonds smooth toward new weights during normal
Yuma Consensus operation. Bonds reset enabled is the separate boolean that gates whether accumulated
bond entries may be cleared when the reset path is active
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus: Bonding mechanics](https://docs.learnbittensor.org/learn/yuma-consensus#bonding)).

- **Bonds moving average** — smoothing speed applied to bonds over time.
- **Bonds reset enabled** — whether bond-reset capability is active on the subnet.

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Glossary: Validator-Miner Bonds](https://docs.learnbittensor.org/resources/glossary#validator-miner-bonds)
- [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)
- [The Subnet Metagraph](https://docs.learnbittensor.org/subnets/metagraph)
