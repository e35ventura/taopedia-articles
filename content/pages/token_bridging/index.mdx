---
title: "Token Bridging"
summary:
  "How token bridging connects TAO between Substrate (SS58) and EVM (H160) account contexts on the
  Bittensor chain, and how vTAO represents staked TAO inside the EVM layer."
category: "Core Concepts"
tags: ["Wallets", "TAO"]
infoboxTitle: Token Bridging
infoboxCaption: Connecting TAO across Substrate and EVM account contexts.
infoboxRows:
  - label: Context
    value: Bittensor EVM
  - label: Connects
    value: SS58 and H160 account formats
  - label: EVM representation
    value: vTAO (staked TAO)
  - label: Article scope
    value: Concept only
---

# Token Bridging

Token bridging is the Bittensor capability that connects TAO across Substrate and EVM account
contexts on the same Bittensor blockchain. The official
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation frames it
as two related ideas: moving the same TAO between SS58 and H160 account formats, and representing
staked TAO as vTAO inside the EVM layer.

In Taopedia terms, token bridging names a connectivity concept, not a wallet type or a subnet role.
It belongs alongside the H160 address concept because bridging is what makes TAO usable inside
Bittensor's EVM environment.

## Why Token Bridging Exists

TAO is the native token of the Bittensor network and is normally held in Substrate-style SS58
wallets. The [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao)
documentation explains that Bittensor also has an EVM layer, and participants need a way to use the
same TAO inside that layer without changing the asset itself.

For readers, token bridging is best understood as the link between two account contexts for one
token. The value does not change when TAO moves between contexts — only the account format and the
set of available operations change.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[Wallets, Coldkeys and Hotkeys](https://docs.learnbittensor.org/keys/wallets)

## What Token Bridging Does

Token bridging moves TAO between SS58 and H160 account formats so the same token can participate in
both the Substrate side and the EVM side of the Bittensor chain. The
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation describes
this as transferring the same token between account formats, where SS58 wallets access
Substrate-side operations and H160 wallets access EVM-side operations.

The second part of token bridging is vTAO, the EVM representation of staked TAO. The documentation
describes vTAO as representing TAO that has been staked, where the vTAO balance a holder holds stays
the same while the underlying staked TAO grows with staking rewards over time.

For readers, token bridging is the concept that keeps TAO usable across both account contexts and
across the staked representation that the EVM layer uses.

## SS58 and H160 as Parallel Account Contexts

Token bridging assumes two account formats for the same on-chain TAO, not two different assets. The
official bridging documentation describes moving TAO between SS58 wallets that access Substrate-side
operations and H160 wallets that access EVM-side operations on the Bittensor chain.

Readers should treat the formats as context switches for the same token. Bridging changes which
operations are available — Substrate extrinsics versus EVM contract calls — without creating a new
asset or an off-chain IOU. Wallet type, address conversion, and bridge tooling belong in official
EVM tutorials and live wallet state rather than in a static article quote. A participant bridging
toward EVM use still holds TAO on the Bittensor chain; only the callable interface changes with the
destination account format.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[H160 Address](https://docs.learnbittensor.org/evm-tutorials/convert-h160-to-ss58)

## vTAO as Staked Representation in EVM

The bridging documentation introduces vTAO as the EVM representation of staked TAO. That distinction
matters because vTAO is not a generic wrapped TAO balance; it tracks TAO that has already entered
the staking model described in official delegation documentation.

For readers, the useful separation is between liquid TAO bridged for EVM use and staked TAO
represented as vTAO inside the EVM layer. Bridging into vTAO is therefore tied to staking context,
not to every EVM transfer of liquid TAO. The vTAO balance a holder sees can stay numerically stable
while the underlying staked TAO grows with staking rewards over time, as described in the token
bridging tutorial. Reward accrual and bridge representation are related but not identical concepts.
Operational bridging steps, fee parameters, and supported conversion paths belong in the official
EVM tutorials rather than in this concept article. Readers should consult those tutorials for
current bridge mechanics.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[Staking and delegation overview](https://docs.learnbittensor.org/staking-and-delegation/delegation)

## Bittensor-Native EVM Scope

Official token bridging documentation notes that Bittensor EVM smart contracts execute on the
Bittensor blockchain itself, not on the Ethereum blockchain. Token bridging therefore moves TAO into
an EVM account context that remains inside Bittensor's own execution environment.

That scope boundary prevents a common misread. Bridging here is not an export of TAO to Ethereum
mainnet or an external L2; it is connectivity between Substrate and EVM layers on the same chain.
Smart contracts invoked after bridging still execute under Bittensor EVM rules described in official
documentation. Readers evaluating bridge use cases should confirm they mean Bittensor's integrated
EVM layer rather than an external network bridge. Contract deployment, bridge interfaces, and H160
wallet setup remain operational topics in official EVM documentation.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[Bittensor EVM Smart Contracts](https://docs.learnbittensor.org/evm-tutorials)

## Relationship to H160 Address

Token bridging and the H160 address are related because H160 is the destination account format for
moving TAO into the EVM context. The
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation describes
moving TAO into Ethereum-style H160 wallets to use it in the EVM layer, and the
[H160 address](https://docs.learnbittensor.org/evm-tutorials/convert-h160-to-ss58) documentation
describes the format itself.

For readers, the H160 address names the EVM-side account format, while token bridging names the
capability of moving TAO into that format. The two concepts work together — the address is the
destination, and the bridge is how TAO reaches it — but they are not the same idea.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[H160 Address](https://docs.learnbittensor.org/evm-tutorials/convert-h160-to-ss58)

## Relationship to Staking

Token bridging and staking are related because vTAO, the EVM representation used in bridging, is a
staked form of TAO. The
[Staking and delegation overview](https://docs.learnbittensor.org/staking-and-delegation/delegation)
describes staking as committing TAO to support a validator and participate in rewards, and the
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation describes
vTAO as representing staked TAO inside the EVM layer.

For readers, staking names the act of committing TAO for rewards, while token bridging names how the
resulting staked TAO is represented when it moves into the EVM context. The two concepts connect
through vTAO but should not be read as the same operation.

References:
[Staking and delegation overview](https://docs.learnbittensor.org/staking-and-delegation/delegation),
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao)

## Relationship to Emission

Token bridging and emission are related because the staked TAO behind vTAO grows with staking
rewards over time. The [Emissions](https://docs.learnbittensor.org/learn/emissions) overview
describes how staked TAO earns rewards, and the
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation notes that
the underlying TAO represented by vTAO increases with those rewards.

For readers, emission names the reward flow that grows staked TAO, while token bridging names the
EVM-side representation that tracks that growth through vTAO. The reward mechanism and the bridge
representation are separate concepts that connect through the staked asset.

References: [Emissions](https://docs.learnbittensor.org/learn/emissions),
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao)

## Relationship to Bittensor EVM Smart Contracts

Token bridging is what makes TAO usable inside Bittensor's EVM smart-contract layer. The
[Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao) documentation notes that
Bittensor EVM smart contracts are executed solely on the Bittensor blockchain, not on the Ethereum
blockchain.

For readers, this keeps the EVM concept scoped to Bittensor. Token bridging moves TAO into the H160
account context so it can interact with Bittensor's own EVM contracts. The bridge connects TAO to
the EVM layer; it does not send TAO to a separate Ethereum network.

References: [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao),
[Bittensor EVM Smart Contracts](https://docs.learnbittensor.org/evm-tutorials)

## Reader Boundary

This article defines token bridging and vTAO as concepts. It does not describe bridging steps,
wallet setup, exchange rates, tool-specific instructions, or supported chain lists. Readers should
use the official Bittensor EVM documentation for operational detail and current parameters.

## Further Reading

- [Token Bridging](https://docs.learnbittensor.org/evm-tutorials/bridge-vtao)
- [Bittensor EVM Smart Contracts](https://docs.learnbittensor.org/evm-tutorials)
- [H160 Address](https://docs.learnbittensor.org/evm-tutorials/convert-h160-to-ss58)
- [Staking and delegation overview](https://docs.learnbittensor.org/staking-and-delegation/delegation)
