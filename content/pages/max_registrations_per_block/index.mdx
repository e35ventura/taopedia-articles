---
title: "Max Registrations Per Block"
summary:
  "How the max registrations per block subnet hyperparameter named a per-block cap on neuron
  registrations under older registration models."
category: "Subnets"
tags: ["Subnets", "Registration"]
infoboxTitle: Max Registrations Per Block
infoboxCaption: Legacy per-block registration cap vocabulary.
infoboxRows:
  - label: Parameter
    value: MaxRegistrationsPerBlock
  - label: Type
    value: u16
  - label: Default
    value: "1"
  - label: Example
    value: netuid 1
  - label: Set by
    value: Root
---

# Max Registrations Per Block

Max registrations per block is a per-subnet hyperparameter, listed in the documentation as
`MaxRegistrationsPerBlock`, that once named a per-block ceiling on neuron registrations. The
reference now states it is not used for neuron registration; live admission is governed by
continuous burn pricing through burn half life and burn increase mult instead
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controlled

Under older registration models, the hyperparameter limited how many UID slots could be claimed in a
single block on a subnet. That cap shaped burst registration behavior separately from the dynamic
burn band and from network-wide rate limits
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)).

It did not set registration price, immunity rules, or subnet capacity. It only bounded how many
successful registrations could land in one block when that per-block cap model was active.

## Current Registration Economics

Official documentation states that neuron registration is burn-based and always open on ordinary
subnets, with admission governed by continuous burn pricing rather than per-block registration caps.
Readers encountering max registrations per block in older material should treat it as legacy
vocabulary and verify live behavior through the current burn hyperparameters on the relevant netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Understanding Neurons](https://docs.learnbittensor.org/learn/neurons),
[Miner registration](https://docs.learnbittensor.org/miners#miner-registration)).

## Documented Type and Default

The [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
reference lists `MaxRegistrationsPerBlock` as a `u16` with a default of one and marks the permission
required to set it as root
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Distinction from Network Rate Limit

Network rate limit is a root-set block-counted wait between subnet-market creations. Max
registrations per block named a per-block cap on neuron registrations within one subnet under older
models ([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Network rate limit** — wait between creating new subnet markets.
- **Max registrations per block** — legacy per-block neuron registration cap vocabulary.

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Glossary: Register](https://docs.learnbittensor.org/resources/glossary#register)
- [Miner registration](https://docs.learnbittensor.org/miners#miner-registration)
- [Rate Limits](https://docs.learnbittensor.org/learn/chain-rate-limits)
