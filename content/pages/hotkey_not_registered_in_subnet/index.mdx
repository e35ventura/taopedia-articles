---
title: "Hotkey Not Registered In Subnet"
summary:
  "How the HotKeyNotRegisteredInSubNet error blocks subnet-scoped extrinsics when the signing hotkey
  does not hold a UID slot on the targeted netuid."
category: "Wallets"
tags: ["Wallets", "Errors"]
infoboxTitle: Hotkey Not Registered In Subnet
infoboxCaption: Error raised when a hotkey lacks a UID on the specified subnet.
infoboxRows:
  - label: Error
    value: HotKeyNotRegisteredInSubNet
  - label: Trigger
    value: Signed call targets a netuid where the hotkey has no UID slot
  - label: Checked in
    value: Subnet-scoped extrinsics such as set_weights
  - label: Check type
    value: Per-subnet UID slot membership
---

# Hotkey Not Registered In Subnet

`HotKeyNotRegisteredInSubNet` means the signing hotkey does not hold a UID on the netuid the call
targets. The standard-errors reference describes it as "The hotkey is not registered in the
specified subnet"
([Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#hotkeynotregisteredinsubnet)).

Subnet membership is scoped per hotkey and netuid. A hotkey registered on one subnet still fails
here when the extrinsic names a different netuid where it has no slot.

## What It Means

Many extrinsics name a `netuid` and require the signing hotkey to already occupy a neuron slot on
that subnet. `HotKeyNotRegisteredInSubNet` means that membership lookup failed for the caller and
targeted subnet, so the extrinsic stops before later stake, weight, or commit logic runs
([Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#hotkeynotregisteredinsubnet)).

On weight and commit paths this is an early gate: the chain checks subnet registration before
minimum-stake, validator-permit, rate-limit, or commit-queue handling.

## Why It Appears

The most direct cause is signing with a hotkey that never registered on the chosen subnet,
registered on a different subnet only, or lost its slot through pruning after the client cached an
outdated UID mapping. Because the check is per `netuid`, a hotkey registered elsewhere still fails
here when the call targets a subnet where it has no slot.

When debugging, rule out association and stake failures on other paths.
`HotKeyAlreadyRegisteredInSubNet` is the opposite case on registration — the hotkey already holds a
UID there. `NonAssociatedColdKey` means the signing coldkey is not paired with the hotkey.
`NotEnoughStakeToSetWeights` means registration passed but stake is too low for weights
([Subtensor Standard Errors: HotKeyAlreadyRegisteredInSubNet](https://docs.learnbittensor.org/errors/subtensor#hotkeyalreadyregisteredinsubnet),
[Subtensor Standard Errors: NonAssociatedColdKey](https://docs.learnbittensor.org/errors/subtensor#nonassociatedcoldkey),
[Subtensor Standard Errors: NotEnoughStakeToSetWeights](https://docs.learnbittensor.org/errors/subtensor#notenoughstaketosetweights)).

The practical meaning is narrow: the signing hotkey is not a neuron on that subnet.

## Distinction from Hotkey Already Registered In Subnet

`HotKeyNotRegisteredInSubNet` and `HotKeyAlreadyRegisteredInSubNet` are opposite outcomes of the
same subnet membership lookup, checked in different extrinsics. `HotKeyNotRegisteredInSubNet` blocks
subnet operations when the hotkey lacks a UID on the targeted `netuid`.
`HotKeyAlreadyRegisteredInSubNet` blocks a new registration attempt when the hotkey already holds
that slot
([Subtensor Standard Errors: HotKeyAlreadyRegisteredInSubNet](https://docs.learnbittensor.org/errors/subtensor#hotkeyalreadyregisteredinsubnet)).

- **HotKeyNotRegisteredInSubNet** — the hotkey has no UID on the subnet the call targets.
- **HotKeyAlreadyRegisteredInSubNet** — registration is redundant because the UID already exists.

## Reader Boundary

This page defines the meaning of the error label and the subnet registration lookup that raises it.
It does not describe how to register a neuron, how pruning removes slots, or global hotkey existence
checks used by other errors; those are documented separately.

## Further Reading

- [Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#hotkeynotregisteredinsubnet)
- [Subtensor Standard Errors: HotKeyAlreadyRegisteredInSubNet](https://docs.learnbittensor.org/errors/subtensor#hotkeyalreadyregisteredinsubnet)
- [Subtensor Standard Errors: NonAssociatedColdKey](https://docs.learnbittensor.org/errors/subtensor#nonassociatedcoldkey)
- [Subtensor Standard Errors: NotEnoughStakeToSetWeights](https://docs.learnbittensor.org/errors/subtensor#notenoughstaketosetweights)
