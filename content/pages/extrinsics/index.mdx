---
title: "Extrinsics"
summary:
  "Extrinsics are submitted Bittensor chain actions that ask the Subtensor runtime to change state
  or perform a supported operation."
category: "Core Concepts"
tags: ["Subtensor", "Transactions"]
---

# Extrinsics

Extrinsics are submitted actions that ask the Bittensor chain to perform a runtime operation. The
official [Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics) reference
lists extrinsic methods exposed by the Subtensor runtime.

For Taopedia readers, the useful idea is that an extrinsic is a state-changing chain action, not a
passive read. Reading chain data is different from submitting an action that the runtime must
process.

## How Extrinsics Fit

Extrinsics are the transaction-like side of interacting with Subtensor. They are used when software
or a user asks the chain to update state through a supported runtime call.

The official [transaction fees](https://docs.learnbittensor.org/learn/fees) page is useful context
because it separates chain reads from state-changing actions. Chain reads are free, while
state-changing actions can carry fees depending on the operation.

## Why the Distinction Matters

The read-versus-submit distinction helps explain why some Bittensor actions only inspect current
state, while others ask the chain to record a change. An extrinsic belongs to the second group: it
is evaluated by the runtime and can affect chain state if accepted.

This article does not try to catalog every extrinsic. The official reference is the right source for
the current method list and exact behavior.

## Common Bittensor Extrinsics

Several Bittensor operations that participants encounter directly are extrinsics. Subnet
registration is a state-changing action that claims a UID slot in a subnet; the official
[Mining](https://docs.learnbittensor.org/miners) reference describes registration as the step that
places a miner into a subnet, and that step is processed by the Subtensor runtime as an extrinsic.
Validator weight submission is another: the official
[Validators](https://docs.learnbittensor.org/validators) reference describes validators as
submitting weights that report their evaluations of miners, and that submission is recorded on chain
through an extrinsic.

Stake operations — staking, unstaking, and delegation — are also extrinsics because they change the
on-chain balance and delegation state. The official
[Staking and Delegation](https://docs.learnbittensor.org/staking-and-delegation/delegation) overview
describes these actions in terms of on-chain effects, which confirms that they belong to the
extrinsic category rather than the passive-read category.

References: [Subtensor extrinsics](https://docs.learnbittensor.org/subtensor-api/extrinsics),
[Mining](https://docs.learnbittensor.org/miners),
[Staking and Delegation](https://docs.learnbittensor.org/staking-and-delegation/delegation)

## Further Reading

- [Subtensor extrinsics reference](https://docs.learnbittensor.org/subtensor-api/extrinsics)
- [Transaction Fees](https://docs.learnbittensor.org/learn/fees)
