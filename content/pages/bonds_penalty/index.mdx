---
title: "Bonds Penalty"
summary:
  "How the bonds penalty subnet hyperparameter sets the magnitude of the consensus penalty applied
  when validator weights exceed the subnet consensus benchmark in Yuma Consensus."
category: "Consensus"
tags: ["Consensus", "Subnets"]
infoboxTitle: Bonds Penalty
infoboxCaption: Out-of-consensus bond penalty magnitude.
infoboxRows:
  - label: Parameter
    value: BondsPenalty
  - label: Type
    value: u16
  - label: Default
    value: "0"
  - label: Example
    value: netuid 1
  - label: Set by
    value: Subnet owner
---

# Bonds Penalty

Bonds penalty is a per-subnet hyperparameter, listed in the documentation as `BondsPenalty`, that
sets the magnitude of the penalty applied when validator weights exceed the subnet consensus
benchmark in [Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus). The
hyperparameter reference describes it as the magnitude of the penalty subtracted from weights for
exceeding consensus and points readers to the out-of-consensus bond penalty section of the Yuma
Consensus documentation
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## What It Controls

Bonds penalty tunes how strongly validator bond-weights are pulled toward the subnet consensus
benchmark when a submitted weight exceeds that benchmark. It sits on the bonding path after clipping
defines the consensus ceiling, before instant bonds and the bond EMA update
([Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds),
[Yuma Consensus: Bonding mechanics](https://docs.learnbittensor.org/learn/yuma-consensus#bonding-mechanics)).

The hyperparameter does not replace clipping or rank calculation. Clipping caps emissions on excess
weight; bonds penalty shapes the bond-weight input that feeds smoothed validator-miner bonds and,
downstream, validator dividend share
([Yuma Consensus: Clipping](https://docs.learnbittensor.org/learn/yuma-consensus#clipping),
[Yuma Consensus: Validator emissions](https://docs.learnbittensor.org/learn/yuma-consensus#validator-emissions)).

## Bond-Weight Interpolation

When a validator's weight for miner `j` exceeds that miner's consensus benchmark `W̄_j`, Yuma
documentation adjusts the bond-weight used in bonding toward that benchmark. The documented form is:

`W̃_ij = (1 − β) · W_ij + β · W̄_j`

where `W_ij` is the validator's submitted weight, `W̄_j` is the consensus benchmark, and `β` is the
penalty factor drawn from the subnet's bonds-penalty hyperparameter
([Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds)).

At `β = 0`, bond-weights keep the submitted values. As `β` rises, bond-weights are pulled further
toward the consensus benchmark before instant bonds and the bond EMA run. That makes
out-of-consensus evaluations cost influence on the bond path even when clipping already limited
emission on the excess portion
([Yuma Consensus: Bonding mechanics](https://docs.learnbittensor.org/learn/yuma-consensus#bonding-mechanics)).

## Documented Type, Default, and Setter

The [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
reference lists `BondsPenalty` as a `u16` with a default of `0`. The stored integer is normalized to
the `β` fraction used in the bond-weight formula by dividing by the type maximum, so the default
means no consensus pull on the bond path until a subnet owner raises the value
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds)).

It is changed through the `sudo_set_bonds_penalty` extrinsic, and the reference marks the permission
required to set it as subnet owner
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

Because each subnet carries its own hyperparameters, the penalty magnitude on one netuid can differ
from another subnet's configuration
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Chain Reads for netuid 1

Readers can verify live hyperparameter values for the documented example netuid with
`btcli subnet hyperparameters --netuid 1 --network finney`
([Subnet Hyperparameters: View hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#view-hyperparameters)).

That read path keeps live hyperparameter claims tied to a parseable netuid.

## Distinction from Clipping

[Clipping](https://docs.learnbittensor.org/learn/yuma-consensus#clipping) removes validator weights
above the stake-backed consensus benchmark so neither the miner nor the validator receives emissions
for the excess portion. Bonds penalty adjusts bond-weight calculation when a validator's submitted
weight exceeds consensus, before smoothed validator-miner bonds are updated
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

- **Clipping** — caps excess weight so emissions are not paid on the clipped portion.
- **Bonds penalty** — pulls bond-weight toward consensus when a weight exceeds the benchmark.

## Distinction from Kappa

[Kappa](https://docs.learnbittensor.org/subnets/subnet-hyperparameters#kappa) sets how much
stake-weighted validator support a weight level must reach before it becomes the clipping ceiling
during Yuma Consensus. Bonds penalty names the out-of-consensus bond penalty magnitude, not the
stake fraction used to compute the clipping benchmark
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Kappa** — stake fraction that sets the clipping ceiling benchmark.
- **Bonds penalty** — magnitude pulling bond-weight toward consensus when weights exceed it.

## Distinction from Bonds Moving Average

Bonds penalty sets the magnitude of the penalty applied when validator weights exceed the subnet
consensus benchmark, while bonds moving average sets the moving-average behavior applied to
validator-miner bonds
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

- **Bonds penalty** — the penalty magnitude for out-of-consensus weights.
- **Bonds moving average** — the smoothing applied to validator-miner bonds over time.

## Distinction from Liquid Alpha

Bonds penalty sets the magnitude of the penalty applied when validator weights exceed the subnet
consensus benchmark, while liquid alpha varies the bond-smoothing factor by consensus alignment
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Consensus-based Weights](https://docs.learnbittensor.org/concepts/consensus-based-weights)).

- **Bonds penalty** — the penalty magnitude applied to out-of-consensus weights.
- **Liquid alpha** — varies how quickly validator-miner bonds smooth, by consensus alignment.

## Distinction from Validator Weights

Validator weights are the evaluation inputs a validator submits for miners on a subnet. Bonds
penalty configures how strongly out-of-consensus weights are pulled toward the consensus benchmark
before smoothed validator-miner bonds update
([Glossary: Validator Weights](https://docs.learnbittensor.org/resources/glossary#weight-vector),
[Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds)).

- **Validator weights** — the submitted evaluation signal for miners.
- **Bonds penalty** — the hyperparameter magnitude for out-of-consensus bond-path adjustment.

## Distinction from Yuma Consensus

Bonds penalty sets how strongly out-of-consensus validator weights are pulled toward the consensus
benchmark during bonding. That hyperparameter magnitude is an input inside settlement, not the full
tempo pass that credits emission shares
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters),
[Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds)).

Yuma Consensus reads the weight matrix, applies clipping and bonding, and credits miner incentives
and validator dividends when the epoch closes
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus),
[Emission](https://docs.learnbittensor.org/learn/emissions)).

Clipping removes extreme outliers before bonding adjusts remaining signals. Bonds penalty names one
tunable strength on that bond path; Yuma names the complete aggregation step on the netuid
([Yuma Consensus: Clipping](https://docs.learnbittensor.org/learn/yuma-consensus#clipping)).

- **Bonds penalty** — hyperparameter magnitude for out-of-consensus bond-path adjustment.
- **Yuma Consensus** — epoch-boundary settlement from filtered weights to emission shares.

## Distinction from Yuma Consensus 3

Yuma Consensus 3 refines bond scaling inside Yuma Consensus. Bonds penalty sets the hyperparameter
magnitude applied when validator weights exceed the consensus benchmark
([How Yuma Consensus 3 Makes Bittensor More Fair](https://docs.learnbittensor.org/learn/yc3-blog),
[Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

- **Yuma Consensus 3** — bond-scaling refinement inside consensus aggregation.
- **Bonds penalty** — penalty magnitude on weights above the consensus benchmark.

Settlement-refinement vocabulary and hyperparameter vocabulary answer different configuration layers
([Yuma Consensus](https://docs.learnbittensor.org/learn/yuma-consensus)).

## Reader Boundary

Bonds penalty should not be read as clipping, liquid alpha, or a live penalty outcome on a miner. It
names only the subnet hyperparameter that sets how strongly out-of-consensus bond-weight is pulled
toward the consensus benchmark on the selected netuid
([Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)).

## Further Reading

- [Subnet Hyperparameters](https://docs.learnbittensor.org/subnets/subnet-hyperparameters)
- [Yuma Consensus: Penalizing out-of-consensus bonds](https://docs.learnbittensor.org/learn/yuma-consensus#penalizing-out-of-consensus-bonds)
- [Yuma Consensus: Clipping](https://docs.learnbittensor.org/learn/yuma-consensus#clipping)
- [Glossary: Validator-Miner Bonds](https://docs.learnbittensor.org/resources/glossary#validator-miner-bonds)
