---
title: "Contract Trapped"
summary:
  "How the ContractTrapped error signals that contract execution trapped before completing normally."
category: "Smart Contracts"
tags: ["Smart Contracts", "Errors", "EVM"]
infoboxTitle: Contract Trapped
infoboxCaption: Error raised when contract execution traps during a call.
infoboxRows:
  - label: Error
    value: ContractTrapped
  - label: Pallet
    value: contracts
  - label: Trigger
    value: Contract execution traps during the call
  - label: Check type
    value: Runtime trap during contract execution
---

# Contract Trapped

`ContractTrapped` is a contracts pallet error raised when contract execution traps. The API errors
reference summarizes it as "Contract trapped during execution"
([Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)).

## What It Means

Bittensor runs an Ethereum-compatible execution layer on Subtensor for smart contracts
([Bittensor EVM Smart Contracts](https://docs.learnbittensor.org/evm-tutorials)). `ContractTrapped`
means contract execution trapped during the call rather than completing normally
([Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)).

This is a trap outcome during execution. It does not, by itself, mean no code exists at a supplied
code hash or that execution exhausted its gas limit.

## Why It Appears

This error appears when contract execution traps during a call
([Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)).

## Distinction from Out Of Gas

`ContractTrapped` and `OutOfGas` are different contracts pallet errors with different API summaries.
`ContractTrapped` is summarized as "Contract trapped during execution." `OutOfGas` is summarized as
"The executed contract exhausted its gas limit."
([Subtensor API Errors: OutOfGas](https://docs.learnbittensor.org/subtensor-api/errors#outofgas),
[Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)).

- **ContractTrapped** — contract trapped during execution.
- **OutOfGas** — the executed contract exhausted its gas limit.

## Distinction from Code Not Found

`ContractTrapped` and `CodeNotFound` are different contracts pallet errors with different API
summaries. `ContractTrapped` is summarized as "Contract trapped during execution." `CodeNotFound` is
summarized as "No code could be found at the supplied code hash."
([Subtensor API Errors: CodeNotFound](https://docs.learnbittensor.org/subtensor-api/errors#codenotfound),
[Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)).

- **ContractTrapped** — contract trapped during execution.
- **CodeNotFound** — no code at the supplied code hash.

## Reader Boundary

This page defines the meaning of the error label from the API reference. It does not diagnose a
specific contract bug or report live code hashes on a network
([Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped),
[Bittensor Networks](https://docs.learnbittensor.org/concepts/bittensor-networks)).

## Further Reading

- [Subtensor API Errors: ContractTrapped](https://docs.learnbittensor.org/subtensor-api/errors#contracttrapped)
- [Subtensor API Errors: OutOfGas](https://docs.learnbittensor.org/subtensor-api/errors#outofgas)
- [Subtensor API Errors: CodeNotFound](https://docs.learnbittensor.org/subtensor-api/errors#codenotfound)
- [Bittensor EVM Smart Contracts](https://docs.learnbittensor.org/evm-tutorials)
