---
title: "Max Weight Exceeded"
summary:
  "How the MaxWeightExceeded error signals that a set_weights call submitted at least one upscaled
  weight value larger than the subnet's max_weight_limit."
category: "Validation"
tags: ["Validation", "Errors"]
infoboxTitle: Max Weight Exceeded
infoboxCaption:
  Error raised when an upscaled weight value in a set_weights submission exceeds the subnet limit.
infoboxRows:
  - label: Error
    value: MaxWeightExceeded
  - label: Trigger
    value: An upscaled weight value exceeds the subnet's max_weight_limit
  - label: Checked in
    value: max_weight_limited
  - label: Check type
    value: Per-element upper-bound check on the upscaled weight vector
---

# Max Weight Exceeded

`MaxWeightExceeded` is a documented Subtensor standard error. The standard-errors reference
describes it as "The dispatch is attempting to set weights on chain with weight value exceeding the
configured max weight limit (currently `u16::MAX`)"
([Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#maxweightexceeded)).

## What It Means

A validator submits a weight vector that names the relative score it wants to assign to each target
UID on the subnet. After the runtime upscalse that vector to fill the `u16` range, every weight must
fit inside the subnet's configured `max_weight_limit`
([Glossary: Weight Vector](https://docs.learnbittensor.org/resources/glossary#weight-vector),
[Glossary: Weight Setting](https://docs.learnbittensor.org/resources/glossary#weight-setting)).

`MaxWeightExceeded` means at least one upscaled weight in the submitted vector was larger than that
subnet limit, so the chain refuses to record the submission
([Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#maxweightexceeded),
[Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

## Why It Appears

The check is the last step before the upscaled weights are zipped for storage. `do_set_weights`
first max-uwpscales the submitted `values` to a `Vec<u16>` of the same length and then calls
`Self::max_weight_limited(netuid, neuron_uid, &uids, &max_upscaled_weights)`. The runtime raises
`MaxWeightExceeded` when that helper returns false
([Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

`max_weight_limited` short-circuits to true in two cases. It returns true when the submission is a
self-weight (a single UID equal to the caller's own UID), so a self-stake on its own subnet is
always allowed. It also returns true when the subnet's `max_weight_limit` equals `u16::MAX`, which
the comment notes is the current constant for that hyperparameter; in that case no upper bound is
enforced. Otherwise the helper calls `check_vec_max_limited(weights, max_weight_limit)` and returns
its result, so any upscaled weight above the limit fails the check
([Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

The same flow runs in `do_set_mechanism_weights`, where the same helper raises `MaxWeightExceeded`
for a mechanism-targeted submission whose upscaled weights breach the subnet limit
([Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

For a reader, the practical meaning is simple: after the runtime normalised the validator's relative
scores to the `u16` range, at least one of them was still above the subnet's configured cap. The
chain refuses the submission instead of recording weights that violate that cap
([Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

## Distinction from Weight Vec Length Is Low

`MaxWeightExceeded` and `WeightVecLengthIsLow` both reject a `set_weights` call from the same
function, but they inspect different properties of the submission. `WeightVecLengthIsLow` rejects
the call because the number of weights is below the subnet's minimum count. `MaxWeightExceeded`
rejects the call because the values of the weights that were submitted are above the subnet's
per-element cap after upscaling
([Subtensor Standard Errors: WeightVecLengthIsLow](https://docs.learnbittensor.org/errors/subtensor#weightveclengthislow),
[Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)).

- **WeightVecLengthIsLow** — the submission does not contain enough weights.
- **MaxWeightExceeded** — the submission contains enough weights, but at least one upscaled value
  exceeds the per-element cap.

## Reader Boundary

This page defines the meaning of the `MaxWeightExceeded` error label and the per-element upper bound
that raises it inside `max_weight_limited`. It does not describe the duplicate-UID check, the
target-existence check, the length check, the version-key check, or the rate-limit checks that
surround `set_weights`; those are separate conditions with their own error labels and their own
articles
([Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs),
[Subtensor Standard Errors: WeightVecNotEqualSize](https://docs.learnbittensor.org/errors/subtensor#weightvecnotequalsize)).

## Further Reading

- [Subtensor Standard Errors](https://docs.learnbittensor.org/errors/subtensor#maxweightexceeded)
- [Subtensor Standard Errors: WeightVecLengthIsLow](https://docs.learnbittensor.org/errors/subtensor#weightveclengthislow)
- [Subtensor Standard Errors: WeightVecNotEqualSize](https://docs.learnbittensor.org/errors/subtensor#weightvecnotequalsize)
- [Subtensor source: errors.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/macros/errors.rs)
- [Subtensor source: weights.rs](https://github.com/opentensor/subtensor/blob/main/pallets/subtensor/src/subnets/weights.rs)
- [Glossary: Weight Vector](https://docs.learnbittensor.org/resources/glossary#weight-vector)
- [Glossary: Weight Setting](https://docs.learnbittensor.org/resources/glossary#weight-setting)
