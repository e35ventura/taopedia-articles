---
title: "Subnet 69: ain"
summary:
  "Subnet 69 is a Bittensor subnet whose live subnet view displays the name ain, but it currently
  has no registered public identity record, repository, website, contact, or description."
category: "Subnets"
tags: ["Subnets"]
---

Subnet 69 is a Bittensor subnet slot whose live subnet view displays the name "ain." The Bittensor
CLI reference documents `btcli subnets show` as the command for inspecting a subnet's live
metagraph, and a live Finney check with that command displays netuid 69 as "ain."

The subnet does not currently have a registered public identity record returned by
`btcli subnets get-identity`. The public explorer view also does not list a source repository,
project website, contact address, or project description for SN69.

Because no public implementation repository or project documentation is registered for the subnet,
this page treats Subnet 69 as a limited-metadata slot rather than as a public protocol with
source-backed miner and validator behavior. Readers should verify the live chain view before relying
on this page, because subnet names and identity fields can change over time.

## Current Status

The useful fact about Subnet 69 is its current on-chain display name. The live Bittensor subnet view
shows netuid 69 as "ain," while the dedicated identity query reports that no existing subnet
identity is registered for the netuid.

That means there is no source-backed task, application, or service to describe beyond the subnet's
current registration and display name. If Subnet 69 later publishes a repository, website, contact,
or description through its identity fields, those public materials should become the starting point
for a more detailed article.

## Miner and Validator Roles

No source-backed miner or validator roles can be described from the current public metadata. The
available chain view does not publish an implementation repository or task description that would
explain what miners produce or how validators evaluate their work.

In active subnets, miners and validators usually coordinate around a documented task, and validator
weights are then processed through [[Yuma Consensus]]. For Subnet 69, the safe reference point is
the general [[Mining and Validating]] model rather than a subnet-specific mechanism.

## On-Chain Metadata

Readers can inspect the live subnet view with `btcli subnets show --netuid 69 --network finney`. The
same Bittensor CLI reference documents `btcli subnets get-identity` as the command for subnet
identity information; for netuid 69, that command currently returns no existing subnet identity
record on Finney.

Live SN69 market and registration data is also available on
[TaoStats](https://taostats.io/subnets/69), whose explorer page currently presents SN69 without
project-specific documentation.

## Further Reading

- [Bittensor CLI reference](https://docs.learnbittensor.org/btcli/)
- [Subnet 69 on TaoStats](https://taostats.io/subnets/69)
- [[Yuma Consensus]]
- [[Mining and Validating]]
